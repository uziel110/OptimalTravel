package com.example.optimaltravel.json;

import android.os.Build;

import androidx.annotation.RequiresApi;

import org.json.JSONArray;
import org.json.JSONException;
import org.json.JSONObject;

import java.io.IOException;
import java.util.List;

import static android.icu.lang.UCharacter.GraphemeClusterBreak.L;

class PlaceConverter {
//    @RequiresApi(api = Build.VERSION_CODES.R)
//    static JSONObject receiveJson() throws JSONException, IOException {
//        JSONObject OrderedPlacesListJson = JsonParser.getJson(List.of("",""));
//        JSONArray placesIDList = (JSONArray) OrderedPlacesListJson.get("geocoded_waypoints");
//        //List<String> wayPoints = new ;
//    }
}